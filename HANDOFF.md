# ゆりあTOEIC（TOEIC対策アプリ）引き継ぎメモ

## 背景・目的

TOEIC学習者「ゆりあ」専用の個人用TOEIC対策アプリ。パート別正答率を入力すると、優先度アルゴリズムで弱点を診断し、研究知見（シャドーイング・語彙カバー率・精読先行等）に基づく週次学習メニューを自動生成する。あわせて語彙（フラッシュカード／クイズ）と文法（解説＋4択クイズ）の学習モジュールをアプリ内に内蔵している。

tangocho・Workout_Appなどと違い、**Supabaseなどのクラウド同期は一切なし**。`localStorage`のみで完結する、ゆりあ専用の単一HTMLファイルアプリ（設計仕様書に「v1はゆりあ専用・単一HTML＋localStorage、将来的にSupabase化して汎用マルチユーザー化」という方針が明記されているが、2026-07-31時点ではまだv1のまま）。

## 重要：本体ファイルはどれか（要注意）

フォルダには紛らわしい同名っぽいファイルが複数あるが、**実際に動いているアプリは`yuliaToeic_v1.html`（1,813行）のみ**。

- `index.html`（258バイト）：`yuliaToeic_v1.html`へ0秒リダイレクトする転送ページ。中身はロジックなし。
- `yuriaToeic_v1.html`（310バイト、"Yuria"のスペル、注意：`yulia`ではなく`yuria`）：これも`yuliaToeic_v1.html`への転送ページ。「ファイル名を変更しました」という案内文のみ。
- `yuliaToeic_v1.html`（175,836バイト、"Yulia"のスペル）：**これが本体**。CSS・JS・データがすべて1ファイルに入っている。

経緯：当初「ゆりあ」のローマ字表記を`Yuria`と誤記していたファイル名を`Yulia`（正しいスペル）に変更した際、ホーム画面に追加済みのショートカット（PWA）が壊れないよう、旧ファイル名の場所に自動転送ページを残した（`修正項目リスト.md`の対応済み項目に記載）。`index.html`も同様にトップページからの導線として転送ページになっている。`manifest.json`の`start_url`も`yuliaToeic_v1.html`を指している。

→ **今後コードを触る・機能を調べるときは必ず`yuliaToeic_v1.html`を見ること。** 他の2つのHTMLは編集不要（転送先パスが変わらない限り触る必要なし）。

## 画面構成・機能

初回はハッシュルーティング（`location.hash`）で以下の順に進む。2回目以降（診断履歴が1件以上）はホーム画面が起点になり、画面下部に固定ナビ（🏠ホーム／📅カレンダー／☰週次メニュー／⚙️設定）が常時表示される。

1. **経験ゲート**（`#/input`、`hasTakenToeic`が未設定の場合）：「TOEICを受けたことがあるか」で分岐。
   - 「いいえ」→ **未経験者向け自己診断**（ネイティブ/ビジネス/日常会話/初歩的/ほとんど話せないの5段階＋英検級・学習経験note）。選んだ段階の目安スコアを正答率換算し、8パート全部に同じ値を設定して診断へ。
   - 「はい」→ **入力フォーム**：8パート（P1〜P7B）の正答率スライダー＋目標モード（fixed=固定正答率 / score=目標スコア）＋目標値＋週あたり学習時間(分)。
2. **診断結果**（`#/diagnosis`）：パート別優先度ランキング＋フラグ表示＋7B分岐質問。
3. **週次メニュー**（`#/menu`）：曜日ごとの学習ブロック（最大4ブロック/日、日曜は固定で「速度・実戦」）。
4. **文法**（`#/grammar`, `#/grammar/:unitId`, `#/grammar/:unitId/quiz`, `#/grammar/:unitId/result`, `#/grammar/stats`）：21ユニット・全104問。各ユニットは解説（`{text, example, note}`の配列）→4択クイズ→結果。苦手問題横断復習モードあり。
5. **語彙**（`#/vocab`, `#/vocab/flashcards`, `#/vocab/quiz`, `#/vocab/result`, `#/vocab/stats`）：TSL 1,258語からのフラッシュカード（英→日、タップでめくる）と10問4択クイズ。苦手語復習モードあり。
6. **7B精密診断**（`#/p7b/retest`, `#/p7b/result`）：Part7Bを時間無制限で解き直し、正答率の伸びで「時間切れが原因」か「情報照合が苦手」かを判定。
7. **ホーム**（`#/home`）：2026-09-21にゲーム化（ペット育成）で作り直し。上から「ペットカード（Lv・EXPバー・ひと言・🍚1分クイズボタン）」→「今日のミッション」→語彙/文法ボタン→「今日の学習メニュー」（従来の曜日別ブロック。折りたたみ）。詳細は後述「ゲーム化」。
8. **カレンダー**（`#/calendar`）：月表示、学習した日にドット表示。
9. **設定**（`#/settings`）：文字サイズ（小/標準/大/特大）、効果音・振動のオン/オフ。

すべてモバイル幅（`max-width:480px`）前提でレイアウトされている（通勤スマホ利用が主用途）。

## データ構造（すべて`yuliaToeic_v1.html`内に定数として直接埋め込み）

ファイル内は13セクションに分かれている（`/* ==== N. 見出し ==== */`のコメントで検索可能）。

| # | セクション | 行(目安) | 内容 |
|---|---|---|---|
| 1 | CONFIG | 171–322 | パート定義（問題数等）、目標モード変換表、閾値、モジュール定義（学習法の手順・tipsもここに直接埋め込み）、曜日ラベル、ストリーク節目、自己診断レベル、文字サイズ選択肢、`game`（XP・レベル曲線・ペット段階・ミッション定義・セリフ） |
| 2 | BUILTIN_VOCAB | 325 | TSL 1,258語。`[{"w":"word","m":"日本語訳"}, ...]`の1行JS配列 |
| 3 | GRAMMAR_UNITS | 328–649 | 文法21ユニット。各`{id, title, targetParts, explanation:[{text,example,note}], quiz:[{q,choices,answer,note}]}` |
| 4 | DEFAULT_DATA + save/load | 650–720 | 後述 |
| 5 | 診断エンジン | 721–778 | 優先度計算・フラグ判定 |
| 6 | 週次メニュー生成 | 779–852 | モジュール選定・曜日割り |
| 7 | 語彙モジュール | 853–970 | 出題選定・クイズ生成・フラッシュカード |
| 8 | 文法モジュール | 971–1050 | クイズセッション・正誤記録 |
| 9 | 7B分岐 | 1051–1064 | 自己申告・精密診断 |
| 9.5 | デイリーログ／ストリーク | 1065–1167 | 学習記録・連続日数・カレンダー・統計 |
| 9.6 | ゲーム | 1168–1357 | XP・レベル・ペット・ミッション・コンボ・効果音 |
| 10 | ルーター | 1358–1430 | ハッシュルーティング・下部ナビ |
| 11 | 表示ヘルパー | 1431–1548 | フォーマット関数、ゲーム用の部品（EXPバー・紙吹雪・レベルアップカード等） |
| 12 | 画面描画 | 1549–2179 | 各`render*`関数 |
| 13 | イベント委譲＋起動 | 2180–2309 | `ACTIONS`オブジェクト（クリックのdata-action分岐）、起動処理 |

### 永続化（localStorage）

- 保存キー：`yuliaToeicV1`
- 旧キー（読み取り専用の移行用）：`yuriaToeicV1`（ファイル名変更前のスペルミス時代のキー。`load()`が新キー→無ければ旧キーの順で読み、次回`save()`時に新キーへ移行される）
- `DEFAULT_DATA`の構造：
  ```
  user: { name, parts:{P1..P7B}, goalMode, goalValue, timeBudgetMin, flags, p7bSelfReport,
          p7bPrecisionResult, hasTakenToeic, beginnerProfile }
  history: [ { date, parts, note } ]   // 診断を受けるたびに追記
  progress: {
    vocab:   { wrongCounts:{word:回数}, seenCounts:{word:回数}, quizHistory:[{date,correct,total}] },
    grammar: { unitStatus:{unitId:{completed,bestScore,attempts,lastScore}}, wrongQuestions:{"unitId:index":回数} },
    dailyLog: { "YYYY-MM-DD": {...activity} },
    milestoneState: { streakStartDate, celebrated:[] },
    game: { xp, initialized, petName, bestCombo, missions:{ date, items:[{id,progress,goal,done}], bonusClaimed } },
  }
  settings: { fontScale, soundOn }
  meta: { schemaVersion:1 }
  ```
- `mergeWithDefaults()`が旧データに新フィールドを補完する形でマージするので、CONFIG/DEFAULT_DATAにフィールド追加してもユーザーの既存データは壊れない設計。

### 診断エンジンのロジック（設計仕様書と一致確認済み）

```
優先度 = 問題数 × max(0, 目標正答率 − 現在正答率)
```
- `goalMode:'score'`の場合は`scoreToAccuracyTable`で目標スコア→目標正答率に線形補間変換してから計算。
- フラグ：
  - `p7bTimeSuspect`：P7A正答率 − P7B正答率 ≥ 25%
  - `foundationGate`：P5またはP6の正答率 < 55%
- `runDiagnosis()`が`{ranking, flags, phase}`を返す。`phase`は`foundationGate`発火なら「土台フェーズ」、それ以外は「処理力フェーズ」。

### 週次メニュー生成ロジック

- `pickModuleQueue()`：優先度>0のパートに対応するモジュールのスコアを合算し降順ソート。`foundationGate`発火時は文法・語彙に+50、`p7bTimeSuspect`発火時は精読に+30のボーナス。
- `buildDayBlocks()`：1日あたり最大4ブロック、`moduleQueue`を曜日インデックス分ずらしながら順番に割り当て、5〜15分単位で配分。
- 日曜だけ固定で「速度・実戦」モジュール（`sundayModule`）。
- 文法ブロックは`pickNextGrammarUnit()`で未完了ユニットを進行順に自動選択、語彙ブロックは`#/vocab/quiz`への導線。

## 語彙モジュールの具体的な動き

- 出題選定（`pickQuizWords`）は完全ランダムではなく重み付き抽選：`重み = 間違えた回数×3 + (未出題なら+2) + 1`。間違えた語・未学習語ほど出やすい。
- 4択の作り方（`buildQuizQuestion`）：正解語の意味＋他の語からランダムに3つ誤答を選び、シャッフルして選択肢にする。
- 回答すると`seenCounts`・`wrongCounts`を更新して`save()`。
- 「苦手を復習する」ボタン→`wrongCounts>0`の語だけを対象に、10問ずつのセットで出題（`review.batchSize=10`）。
- フラッシュカードは20語のデッキをタップでめくる形式（別セッション、正誤記録なし）。

## 文法モジュールの具体的な動き

- 21ユニット・104問（`文法項目リスト.md`のv2案を元に全項目実装済み。実装状況は同ファイル末尾に記載）。各ユニットは`title`・対象パート・`explanation`（ポイントごとに解説文＋例文＋補足の配列）・`quiz`（複数問の4択）を持つ。
- ユニット完了時に`unitStatus[unitId]`へ`completed`・`bestScore`（過去最高正答率）・`attempts`・`lastScore`を記録。
- 間違えた設問は`wrongQuestions["unitId:index"]`にカウント。「苦手問題を復習する」は全ユニット横断でこれを集めて10問ずつ出題。

## ゲーム化（ペット育成）— 2026-09-21追加

ユーザーがアプリを使わなくなったため、「開くとペットが待っている → 1分クイズでごはん → 育つ」流れを追加。設定値はすべて`CONFIG.game`。

- **XP**：正解+10、不正解+2（挫折させないため0にしない）。コンボ3/5/10回ちょうどで+5/+10/+20のボーナス。
- **レベル**：`xp`だけを保存し、レベルは毎回`calcLevelInfo()`で計算。Lv nからn+1に必要なXP＝50＋30×(n−1)。
- **ペット**：Lv1たまご→Lv3こねこ→Lv8ねこ→Lv15おしゃれねこ🎀→Lv25マスター👑（emoji＋CSSのみ、画像なし）。表情（`getPetMood`）は今日学習済み=ごきげん／昨日まで=おなかすいた／2日以上空く=しょんぼり。**罰はなし**（死なない・XPは減らない）。
- **1分クイズ**：`startVocabQuiz(5,'quick')`。画面は語彙クイズを流用し、タイトルが「🍚 ごはんタイム」、結果画面は「もう1回」が一番大きいボタン。
- **今日のミッション**：`generateDailyMissions(日付)`が日付をシードにした乱数で3つ選ぶ（同じ日は何度開いても同じ）。同じ`group`（未指定なら`event`）は1日1つまで、「苦手を復習」は苦手が1つ以上あるときだけ候補になる。全達成で+50XP。
- **フック**：`submitVocabAnswer`/`submitGrammarAnswer`→`onAnswer()`、クイズ完了→`onSessionComplete()`。
- **演出**：コンボ表示・正解ボタンが弾む・紙吹雪（5/10コンボ、レベルアップ、ミッション全達成）・ミッション達成トースト・Web Audioで合成した効果音・`navigator.vibrate`（Androidのみ）。レベルアップ等の全画面カードはクイズ中は出さず、結果画面/ホームで表示（`APP_STATE.gameOverlays`キュー）。
- **既存ユーザーの移行**：初回起動時に`initGameState()`が「これまで解いた単語数×2＋完了した文法ユニット数×30」のXPを付与。
- `prefers-reduced-motion`（OSの「視差効果を減らす」設定）ではアニメーションを止める。

## 7Bの精密診断

- 既定は自己申告（「解き切れた？」）。
- 精密診断ボタンでP7Bのみ無制限リテスト。再テスト正答率 − 元の正答率 ≥ 15%なら「時間切れが原因」、それ未満なら「情報照合が苦手」と判定。

## 既知の注意点・未対応事項（`修正項目リスト.md`より、2026-07-31時点で未対応）

- **同じモジュールが連日並んでしまう**：全パートの正答率が同程度だと、`pickModuleQueue`のスコアが僅差になり、モジュール選定ロジックが同じモジュール（例：速度・実戦）を繰り返し選びやすい。未経験者の自己診断（全パート同一正答率）で特に起きやすい。
- **「メニュー再生成」ボタンが実質無反応**：`generateWeeklyMenu`/`pickModuleQueue`/`buildDayBlocks`に乱数要素が一切なく、同じ診断結果・同じ学習進捗なら常に完全に同一の週間メニューを返す仕様になっている。クラッシュはしないが出力が1バイトも変わらないため「反応していない」ように見える（Playwrightで再現確認済みとの記録あり）。上記の「連日同じモジュール」と根本原因は同じ（配分に多様性を持たせる仕組みがない）。

対応するとしたら、`pickModuleQueue`か`buildDayBlocks`に乱数または前回結果との差分ロジックを入れる必要がある。

## 認証・機密情報について

このアプリはSupabase等のクラウドサービスを一切使っておらず、認証機構・APIキー・パスワードの類は**コード上に存在しない**（確認済み）。全データはローカルファイル（HTML内埋め込み＋localStorage）で完結している。

## 保存場所・関連ファイル

- フォルダ：`~/Desktop/TOEIC_app/`
- **本体（唯一のソースファイル）**：`yuliaToeic_v1.html`
- 転送用スタブ（編集不要）：`index.html`、`yuriaToeic_v1.html`
- PWA関連：`manifest.json`、`icon-192.png`、`icon-512.png`、`apple-touch-icon.png`
- 設計仕様書：`TOEIC対策アプリ_設計仕様書_1.md`（診断ロジック・データモデル・コンテンツ方針の原典。実コードと照合確認済み、矛盾なし）
- 修正メモ：`修正項目リスト.md`（対応済み・未対応の不具合一覧。上記「既知の注意点」参照）
- 文法設計資料：`文法項目リスト.md`（21ユニットの文法体系の元ネタ・出典。GRAMMAR_UNITSのidはこの文書のg_〜という命名をそのまま使用）
- 単語データ調査メモ：`TOEIC単語データ説明.md`（BUILTIN_VOCABの由来と、まだアプリに未統合の拡張候補データの説明）
- 語彙データ原本：`toeic_tsl_full_tangocho.json`（BUILTIN_VOCABの元データ。アプリにはHTML内へ直接コピーされた状態で埋め込まれている。このJSON自体は編集してもアプリには自動反映されない＝再埋め込みが必要）
- `Contents/toeic_vocab.csv`（4,060語、NGSL基準。tier T1/T2/T3＋TOEIC分類済みだが、T1〜T3は日本語訳がほぼ空。**まだアプリに統合されていない拡張候補**。統合するには日本語訳を埋めてからJSON化しBUILTINに追加する作業が必要）
- `Contents/toeic_tsl_full_review.csv`（1,259語、`no,word,jp`形式。BUILTIN_VOCABと同じTSL単語リストのレビュー用CSV版）
- `Documents/TOEIC学習法レポート.md`（CONFIG内の各モジュールの学習法・手順・tipsの元ネタとなった研究ベースのレポート）
- `Documents/tsl_11.txt`（TSL 1,258語の生の単語リスト。日本語訳なし、出典データそのもの）
- `Documents/crossref_result.csv`（tangochoの単語帳とTSLの重複分析。1,258語中12語のみ重複=`tangocho_and_TSL`、残り1,246語は`TSL_only_need_to_learn`。設計仕様書§11-1の「重複0.5%」という記述の根拠データ）
- Git管理あり、リモート：`https://github.com/hei86gns/toeic-app.git`

## 機能について質問したいとき

このメモと`yuliaToeic_v1.html`本体があれば、別のフォルダ・別のチャットからでも「クイズの出題ロジックはどうなってる？」「週次メニューの配分を変えたい」「文法ユニットを追加したい」といった質問にその場で答えられる。会話ログそのものは引き継げないが、このメモが実質的に同じ役割を果たす。行番号はファイル更新で多少ずれる可能性があるため、まず本メモのセクション見出し（`/* ==== N. 見出し ==== */`）で検索し直すこと。
