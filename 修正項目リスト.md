# yuliaToeic_v1.html 修正項目リスト

見つかった不具合・改善点をここに書き溜める。まとめて対応するタイミングで着手する。

## 未対応

- [ ] **全パートが同程度の正答率だと、週次メニューの同じモジュールが連日並んでしまう**
  優先度が僅差の場合、モジュール選定ロジック（`pickModuleQueue`/`buildDayBlocks`）が同じモジュール（例：速度・実戦）を繰り返し選びやすい。特に自己診断（未経験者向け）で全パート同一の正答率を設定した場合に発生しやすい。今回の5件とは別件のため、今回は対応せず記録のみ。

- [ ] **週次メニューの「メニュー再生成」ボタンを押しても何も変わらない**
  `generateWeeklyMenu`/`pickModuleQueue`/`buildDayBlocks`には乱数要素が一切なく、同じ診断結果・同じ学習進捗であれば常に完全に同一の週間メニューを返す仕様になっている。そのため「メニュー再生成」ボタンはクラッシュはしないが、実際には出力が1バイトも変わらず「反応していない」ように見える（Playwrightで再現・確認済み：クリック前後でHTMLが完全一致）。上記の「同じモジュールが連日並ぶ」と根本原因は同じ（配分ロジックに多様性を持たせる仕組みがない）。押しても飛び先が変わるわけではなく（「最初から入力し直す」とは別のボタンで、そちらへは遷移しない）、あくまで週の割り振りを組み直すだけの機能。

## 対応済み

- [x] **どの画面からもホーム／カレンダー／週次メニュー／設定に行けない**
  週次メニュー画面の「今日の画面に戻る」を「ホームにもどる」に文言統一。またホーム画面だけにあった⚙️☰📅アイコン行を廃止し、代わりに画面下部に固定の共通ナビ（🏠ホーム／📅カレンダー／☰週次メニュー／⚙️設定、この順）を追加。診断が一度でも完了した後（`DATA.history`がある状態）は、どの画面を開いていても下部に常時表示される。

- [x] **「ゆりあ」のローマ字スペルが誤り（Yuria→正しくはYulia）**
  ファイル名を`yuriaToeic_v1.html`から`yuliaToeic_v1.html`に変更。localStorageの保存キーも`yuriaToeicV1`→`yuliaToeicV1`に変更したが、既存の学習データが消えないよう`load()`で旧キーも読めるように移行処理を追加（次回保存時から自動的に新キーに移る）。旧ファイル名の場所には新ファイルへの自動転送ページを残し、ホーム画面に追加済みのショートカットが壊れないようにした。

- [x] **文法説明に例文がない／例文がクイズの使い回し／説明と例文が離れている**
  当初はクイズ問題の空欄を埋めた文をそのまま例文として使い、別カードに表示していたが、「クイズとは違う例文にしてほしい」「説明のすぐ下に例文を表示してほしい」との要望を受けて再実装。各ユニットの`explanation`を単一の文字列から`{text, example, note}`の配列に変更し、ポイントごとに新規の例文（クイズの設問とは別に書き下ろし、重複なしをテストで確認済み）をその場で表示するようにした（全21ユニット・68ポイント）。

- [x] **クイズの「途中でやめる」ボタンが選択肢に近すぎて誤タップしやすい**
  語彙・文法クイズ両方で、選択肢（または回答後の「次へ」ボタン）と「✕ 途中でやめる」ボタンの間隔を12px→36pxに拡大。

- [x] **語彙・文法それぞれに「間違えた問題を順番に出す」復習モードがない**
  - 語彙：語彙メニューに「苦手を復習する（◯語）」ボタンを追加。`wrongCounts`が1以上の単語を対象に、通常クイズと同じ10問ずつのセットで出題。10問を超える場合は結果画面に「次のセットに進む」ボタンを表示。
  - 文法：文法ユニット一覧に「苦手問題を復習する（◯問）」ボタンを追加。全21ユニットを横断して、これまで間違えた設問（新規追加した`progress.grammar.wrongQuestions`で問題単位に記録）を順番に10問ずつ出題。画面上部にどのユニットの問題かを表示。
  - どちらのモードも「✕ 途中でやめる」ボタンで中断可能（既存のクイズと同じ導線）。

- [x] **週次メニュー画面に「ホーム／診断結果に戻る」導線がない**
  週次メニュー画面（`renderMenu`）に「診断結果に戻る」（`#/diagnosis`）リンクを追加。同じ根本原因（戻る導線の欠如）が診断結果画面（`renderDiagnosis`）にもあったため、そちらにも「最初から入力し直す」（`#/input`）リンクを追加。ブラウザ操作で両画面のリンクが正しく機能することを確認済み。

- [x] **単語クイズ・文法クイズができなくなった**
  実際にはクラッシュではなく、新しいホーム画面（曜日別表示）でその日のローテーションに語彙・文法が含まれないと入口が無くなっていたのが原因。ホーム画面に「語彙をやる」「文法をやる」の常設ボタンを追加し、その日の内容に関わらずいつでも語彙・文法に入れるようにした。

- [x] **最初の画面：TOEICを受けたことがない人向けの分岐**
  入力画面の前に「TOEICを受けたことがありますか？」の質問を追加。「はい」は従来通りパート別正答率の入力へ、「いいえ」は自己診断（ネイティブ/ビジネス/日常会話/初歩的/ほとんど話せないの5段階＋英検級・学習経験は参考情報として収集）に進み、選んだレベルに対応する目安スコア（950/730/550/450/300点）を精度換算した数値を全パートに均等に設定して診断する。

- [x] **文法説明のレイアウトを見やすく**
  解説文を句点で分割し、箇条書き（`.explanation-list`）で表示するように変更。

- [x] **文字サイズを変更できる設定メニュー**
  ホーム画面に⚙️アイコンを追加し`#/settings`画面を新設。「小・標準・大・特大」からフォントサイズを選べ、選択内容はlocalStorageに保存され次回起動時にも反映される（CSS全体をpx指定からrem指定に変更し、ルートのフォントサイズ変更で全体が連動して拡大縮小するように調整）。

- [x] **クイズの途中やめボタン**
  語彙クイズ・文法クイズの両方に「✕ 途中でやめる」ボタンを追加。押すと進行中のセッションを破棄し、語彙メニュー／該当の文法ユニット画面に戻る。
